<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elearningapp.tech5soft.upskill">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CourseDetails.CourseInfoActivity"></activity>
        <activity android:name=".RequisitionActivity" />
        <activity
            android:name=".LoginActivity.LoginActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".DashBoard.DashBoardActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".Search.SearchActivity" />
        <activity android:name=".WishList.WishListActivity" />
        <activity android:name=".CourseDetails.CourseDetailsActivity"
            android:configChanges="orientation|screenSize|layoutDirection"/>
        <activity android:name=".CourseDetails.MoreActivity" />
        <activity
            android:name=".CourseDetails.CourseInformationActivity">

        </activity>
        <activity android:name=".CourseDetails.QAActivity" />
        <activity android:name=".CourseDetails.ResourceActivity" />
        <activity
            android:name=".LoginActivity.SplashActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>